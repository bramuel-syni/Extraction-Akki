# Owner rulings 2026-07-31 · UI-1 Stage A approval + downstream

**Filed:** 2026-07-31 (following Experience Canon v1.0 receipt).
**Standing rule:** SR v3 — verbatim carrier.

---

## Ruling 1 — UI-1 Stage A APPROVED (verbatim)

Owner verbatim: *"UI-1 STAGE A: APPROVED. Start UI-1-A (use_data). Both seal events land in A. Per-sub-cycle independent verification before the next begins; close reports on disk."*

**Applied:** UI-1-A execution begins. Two frozen contracts land in this sub-cycle: `UseDataWizardSession@v0` (parity 34→35) + `CommissionVerdict@v0` (35→36). Close report lands at `docs/close_reports/ui_1_a_use_data_2026-07-31.md` on completion. Owner independent verification precedes UI-1-B (govern) dispatch.

## Ruling 2 — SLOT-4 candidates require verbatim delivery + per-item tag

Owner verbatim: *"SLOT-4: include the 11 pending-fold candidates VERBATIM (not summarized) in your next report to me. Non-blocking for UI-1-A. Owner will batch-fold pure execution detail as Canon v1.1; anything experience-visible gets ruled individually — so tag each of the 11 as [PURE-EXECUTION-DETAIL] or [EXPERIENCE-VISIBLE] to speed the ruling."*

**Applied:** the 11 pending-fold SLOT-4 candidates in `docs/canon_slot4_delta_log_2026-07-31.md` §§1-3 will be re-rendered VERBATIM + per-item tagged in the UI-1-A close report. Non-blocking for this sub-cycle.

## Ruling 3 — B1 GPU spend ceiling (fifth `$[NUMBER]` blank occurrence)

Owner verbatim: *"B1 GPU spend ceiling: the Owner's ruling text read '$[NUMBER]' — the literal figure was AGAIN not filled (fifth occurrence). Record: B1 authorized-on-receipt-of-figure, figure still blank, B1 hardware rental remains blocked. Carry the flag. (Fixture-only; no B1 figure quoted beyond the build record when it starts.)"*

**Applied:**
- B1 hardware rental status: **AUTHORIZED on receipt of the figure**, figure STILL BLANK (5th occurrence).
- B1 hardware rental remains **BLOCKED** until the figure lands.
- No B1 figure quoted beyond the build record when it starts (fixture-only).
- Flag remains carried on every close report until resolution.

## Ruling 4 — OT tracks on Owner clock; UI-1 does not wait

Owner verbatim: *"OT-1a/1b/2/3 on Owner clock; nothing in UI-1 waits."*

**Applied:** OT-1a (RMS-side archive facts) · OT-1b (grant-provider GPU parameters) · OT-2 (Hour A + 300-unit slice) · OT-3 (LLM account + domain + object store + data-plane) remain OPEN on Owner clock. **UI-1 execution proceeds independently.**

═══════════════════════════════════════════════════════════════════

*End of AC-2 ruling record. SR v3 · verbatim carrier.*
