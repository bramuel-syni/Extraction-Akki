# Owner Rulings — Phase 3 approval + re-sequencing (2026-08-01)

**Status:** RECORDED · same-day per AC-2
**Authority:** Owner (message 2026-08-01, post-Cycle-3 close acknowledgment)

---

## Ruling 1 — Phase 3 APPROVED · re-sequenced

**Verbatim:**
> "Phase 3 approved, re-sequenced: sub-cycle 1 = real-material readiness slice (overrides module order). Subsequent: sub-cycle 2 Memory + Registry, then Govern, then Prove + Team — each independently verified."

**Consequence:** Sub-cycle 1 (this cycle) is the real-material readiness slice: Connect module + Use-Data (extraction) door with FB-4/FB-6 envelope + Commit Review + Runs/Run-Detail under Commission View (FB-5). Module order is deliberately overridden — the slice cuts VERTICALLY across Connect + Use-Data to deliver a demo-ready evidence flow ahead of module-complete surfaces.

## Ruling 2 — Plane-observability panel APPROVED-WITH-PHASE-3 (sub-cycle 2)

**Verbatim:**
> "Plane-observability panel: APPROVED-WITH-PHASE-3, rides sub-cycle 2 (Memory module). Not standalone. Do NOT build it in sub-cycle 1."

**Consequence:** Panel is IN scope for sub-cycle 2 (Memory + Registry), OUT of scope for this sub-cycle 1. Filed here for continuity; no code lands this cycle.

## Ruling 3 — Grants-revision JWT claim PARKED (SR-5)

**Verbatim:**
> "Grants-revision JWT claim: PARKED — rules in only on a measured login-latency or load figure, never speculation. Log the parking (SR-5 discipline)."

**Consequence:** No JWT-caching optimization lands until a measured figure (login latency percentile ≥ X ms sustained OR load figure ≥ Y QPS sustained) is produced. Speculation is prohibited (SR-5 fail-loud on unset).

## Ruling 4 — A5-1 binding copy PARTIAL RATIFICATION

**Verbatim:**
> "A5-1 binding copy PARTIAL RATIFICATION — these strings are ratified VERBATIM and must render exactly: the refusal action triplet ('Accept as recorded statement' · 'Narrow the objective' · 'Lower the standard'), 'Frozen is immutable.', and the unset-retention banner. ALL OTHER Appendix A strings remain SUSPENDED — their copy slots must render visibly marked as open (e.g., a marked open-copy-slot treatment), never filled with invented copy."

**Ratified verbatim strings:**
1. Refusal action triplet — three exact strings, no re-punctuation, no rewording:
   - "Accept as recorded statement"
   - "Narrow the objective"
   - "Lower the standard"
2. "Frozen is immutable."
3. Unset-retention banner (verbatim per FB-13):
   > "the system holds everything indefinitely until you set a window — a decision only you can make"

**Suspended (marked-open-slot treatment):** ALL other Appendix A strings render with a visually-marked-open placeholder (e.g., dashed border, muted "— open —" glyph) — NEVER filled with invented copy.

## Ruling 5 — A5-2 Data Engineer role default = Master Admin landing alias

**Verbatim:**
> "A5-2: Data Engineer role defaults to Master Admin landing alias until the Owner supplies the mandate sentence. Record as open item."

**Consequence:** `role=data_engineer` routes to the Master Admin landing surface. Recorded as open item; awaiting Owner mandate sentence for full role definition.

## Ruling 6 — B1 interim GPU spend ceiling AWAITING OWNER FIGURE

**Verbatim:**
> "B1 interim GPU spend ceiling: Owner's message contained an UNSET placeholder '[SET $ HERE]'. Record as AWAITING OWNER FIGURE — required before any B1 hardware rental; gates Phase 2 Stage B hardware only, nothing in this sub-cycle. OT-1a scheduling in motion on Owner side."

**Consequence:** No Phase 2 Stage B hardware rental until Owner supplies the figure. Sub-cycle 1 is not gated by this (frontend + thin backend seams only).

═══════════════════════════════════════════════════════════════════

*Rulings on disk per AC-2 same-day discipline. Cycle 3 close acknowledged; Phase 3 sub-cycle 1 build proceeds.*
