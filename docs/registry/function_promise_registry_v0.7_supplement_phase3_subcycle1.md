# Function & Promise Registry — v0.7 Supplement (Phase 3 sub-cycle 1)

**Purpose:** carries the Phase 3 sub-cycle 1 (real-material readiness slice) close's R4 reflexive Registry rows, landed per governance §14 additive-supplement discipline. AC-3 non-negotiable: FPR rows registered in machine YAML BEFORE each function lands. This supplement authoritatively documents the rows.

**Source lock:** primary source-of-truth `/app/docs/registry/function_promise_registry_v1.md` byte-identical (SHA unchanged). Prior supplements v0.1..v0.6 byte-identical.

**Landed:** 2026-08-01 (atomic Phase 3 sub-cycle 1 commit).

**Governance authority:** Owner ruling 2026-08-01 (Phase 3 approved, re-sequenced — sub-cycle 1 = real-material readiness slice); Ruling 4 (A5-1 binding copy partial ratification); Ruling 1 (Connect stub honestly marked, four designed states).

---

## §T1. Design system + response-class taxonomy

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.frontend.design_law_palette_locked` | Named surfaces (frontend) | Akki v4 palette (cream #F3F2E9 · navy #16304F · oxblood #7E3038 · sage #8A8F7C · amber #B07C2A) frozen in `frontend/src/design/akkiv4_design_system.js`; Georgia serif wordmark; Helvetica labels. All sub-cycle-1 screens import from this single source. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/akkiv4_design_system.js | Jest (test_design_law_and_ratified_copy.test.js: palette hex codes canonical + Object.isFrozen assertion) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.response_class_taxonomy_four_visual` | Named surfaces (frontend) | Four response-class visual treatments distinct: governed_refusal (oxblood) · validation_error (amber) · infrastructure_fault (sage) · access_control_denial (navy). Never conflated per Owner cycle-3 design law. | PROM-S1-refusal-taxonomy-closed | S1.call | frontend/src/design/akkiv4_design_system.js + ResponseClassPanel.jsx | Jest (test: each class has a distinct accent color + distinct test-id + classifyResponse mapping) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.ratified_copy_verbatim` | Named surfaces (frontend) | Refusal action triplet + "Frozen is immutable." + unset-retention banner render byte-identical to Ruling 4 verbatim strings. Frozen tuple; mutation prohibited. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/ratified_copy.js | Jest (test_design_law_and_ratified_copy.test.js: byte-identical string assertions + Object.isFrozen) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.open_copy_slot_marker` | Named surfaces (frontend) | Every suspended Appendix A copy slot renders `<MarkedOpenSlot>` with dashed sage border + "— open —" glyph. No invented copy per Ruling 4. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/MarkedOpenSlot.jsx | Jest (renders marker + slot name + dashed sage style) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.class_with_claim_headline` | Named surfaces (frontend) | Every headline in answer position carries `<ClassBadge>` + claim; claim alone is prohibited. Governed refusal: "Refused · <reason>". Access denial: "Not authorised · <reason>". | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/ResponseClassPanel.jsx | Jest (test-ids `refusal-class-with-claim`, `acl-class-with-claim`, `validation-class-with-claim`, `infra-class-with-claim`) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.refusal_in_answer_position` | Named surfaces (frontend) | Refusals render inline where the answer would render — NOT as modal (no `role=dialog`) and NOT as toast (no `position:fixed`). | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/ResponseClassPanel.jsx | Jest (role attribute is `alert` not `dialog`; position style is not `fixed`) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.agent_assumed_amber_chip` | Named surfaces (frontend) | Agent-assumed fields carry `<AgentAssumedChip>` (amber #B07C2A) + XOR-with-supplied source tag. Draft rail three visual states preserved. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/AkkiShell.jsx + operator draft-rail | Jest (existing test_operator_draft_rail_three_visual_states.test.js — extended-compat) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.one_trace_thread_audit_rail` | Named surfaces (Northena) | Every sub-cycle-1 screen renders `trace_id` in the audit rail (AkkiShell footer). N-INV thread invariant preserved. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/AkkiShell.jsx + ResponseClassPanel.jsx | Jest (`akki-shell-trace-rail` + `*-trace-id` test-ids) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.no_build_state_on_any_surface` | Named surfaces (frontend) | No "coming soon" / loading skeleton framed as capability. Every capability is either lit (measured) or dormant (visibly unlit/hatched via `<DormantCapabilityChip>`). Owner cycle-3 directive verbatim. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/design/AkkiShell.jsx (DormantCapabilityChip) | manual + integration | 1 cell · µs class | React | 1 · Deterministic | Owner |

## §T2. Connect module (thin governed stub)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.connect.module_home_lists_dormant_capabilities_honestly` | Named surfaces (Connect) | `/connect` frontend + `/api/connect/capabilities` backend list source-connection capabilities; each carries "dormant · awaiting OT-1a" chip. No live-connected claim. Four unmeasured dimensions per capability (throughput / latency / cost_per_hour). | PROM-S1-external-scoped-access | S1.call | frontend/src/pages/connect/ConnectHomePage.jsx + backend/routers/connect.py | pytest (test_connect_capabilities_lists_dormant_only) + manual UI | 1 cell · ms class | FastAPI + React | 1 · Deterministic | Owner |
| `akki.connect.new_source_stub_seam_honestly_marked` | Named surfaces (Connect) | `/connect/new` renders the source-registration form + a governed refusal path. Submit refuses with `connect_seam_dormant` envelope. Form present so shape is inspectable; never presented as live. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/connect/ConnectNewSourcePage.jsx | integration (GovernedRefusalCard renders on submit) | 1 cell · ms class | React | 1 · Deterministic | Owner |
| `akki.connect.backend_seam_governed_stub` | Named surfaces (Connect) | `POST /api/connect/sources` refuses with HTTP 501 + `{outcome:"refused", reason:"connect_seam_dormant", detail: ...}`. Honest governed stub; never silent fallback. | PROM-S1-refusal-taxonomy-closed | S1.call | backend/routers/connect.py | pytest (test_connect_post_sources_refuses_governed) | 1 cell · µs class | FastAPI | 1 · Deterministic | Owner |

## §T3. Wizard extension (FB-4 milestones + FB-6 lawful basis)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.wizard.milestone_capture_step_landed` | Named surfaces (Wizard) | Commission wizard gains a milestone-capture panel below the chat pane. Each milestone: description + done-condition + owner + order-index + status. Persisted in sidecar collection `wizard_session_milestones` — WizardCommitState_v0 frozen snapshot byte-identical. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/operator/CommissionWizardPage.js + backend/services/wizard/milestones.py + backend/routers/wizard_operator.py milestone endpoints | pytest (test_wizard_milestones_endpoints_land) | 1 cell · ms class | Motor + FastAPI + React | 1 · Deterministic | Owner |
| `akki.wizard.milestone_agreement_gates_commit_cta` | Named surfaces (Wizard) | Commit CTA is DISABLED until (a) milestone list agreed AND (b) lawful_basis supplied. Anti-laundering: propose resets agreed flag. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/operator/CommissionWizardPage.js `commitCtaDisabled` + backend freeze gate | pytest (test_wizard_freeze_refuses_when_milestones_not_agreed + test_wizard_milestones_propose_resets_agreed_flag) | 1 cell · ms class | React + FastAPI | 1 · Deterministic | Owner |
| `akki.wizard.lawful_basis_mandatory_at_commit` | Named surfaces (Wizard) | Lawful basis is a mandatory field per Guard 1 (pre-existing) + rendered as REQUIRED in draft rail. Freeze refuses with mandatory-field violation when empty. | PROM-S1-frozen-wire-contract | S1.call | contracts/wizard_commit_state.py Guard 1 (pre-existing) + frontend draft-lawful-basis red highlight | pytest (test_gate_four_response_class_envelope_shapes_distinct implicitly + existing wizard Guard 1 tests) | 1 cell · ms class | React + FastAPI | 1 · Deterministic | Owner |
| `akki.wizard.commit_review_separates_supplied_from_assumed` | Named surfaces (Wizard) | Commit review renders `you_supplied` vs `agent_assumed_items` per-value with confirm-or-change buttons. Source XOR discipline. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/operator/CommitReviewPage.js (pre-existing) + draft-rail three-state gate | Jest (test_operator_draft_rail_three_visual_states.test.js: three visual states coexist) | 1 cell · ms class | React | 1 · Deterministic | Owner |

## §T4. Commission View (FB-5 · reads only existing artifacts)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.commission_view.milestone_front_page_landed` | Named surfaces (Commission-View) | `/commission-view/:sessionId` renders milestone checklist + spend-vs-quote at the top level. Front page discipline (FB-5). | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/commission_view/CommissionRunDetailPage.jsx `MilestoneFrontPage` | integration + manual UI (renders test-ids `commission-milestone-front-page` + `milestone-list` + `spend-vs-quote-row`) | 1 cell · ms class | React | 1 · Deterministic | Owner |
| `akki.commission_view.reads_only_existing_artifacts` | Named surfaces (Commission-View) | No new backend compute path. Reads existing wizard-operator `GET /api/wizard/operator/{sid}` + milestone sidecar `GET /api/wizard/operator/{sid}/milestones` only. No new hot-path SQL. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/commission_view/CommissionRunDetailPage.jsx | manual (endpoint inventory — no new commission-view backend route) | 1 cell · ms class | axios | 1 · Deterministic | Owner |
| `akki.commission_view.missed_milestone_renders_as_plainly_as_done` | Named surfaces (Commission-View) | Missed styled with same visual weight (fontWeight 700) as done, distinct color (oxblood vs green). Never hidden. Never soft-toned. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/commission_view/CommissionRunDetailPage.jsx STATUS_STYLE map | inspection (STATUS_STYLE map + Jest snapshot pending) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.commission_view.drill_down_evidence_never_leads` | Named surfaces (Commission-View) | Drill-down evidence lives inside `<details>` element (progressive disclosure). Milestone panel + spend-vs-quote render ABOVE. FB-5 discipline. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/commission_view/CommissionRunDetailPage.jsx `<details data-testid="commission-detail-evidence-toggle">` | inspection (DOM order) | 1 cell · µs class | React | 1 · Deterministic | Owner |

## §T5. Backend seams (wizard state additive extension)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.backend.wizard_state_carries_milestones_via_sidecar` | Named surfaces (Wizard) | Milestone list persisted in sidecar collection `wizard_session_milestones` keyed by session_id. WizardCommitState_v0 frozen contract UNCHANGED (byte-identical snapshot). D4b assessment: internal state; no env boundary; LIVE prior — implemented as sidecar rather than contract extension. | PROM-S1-frozen-wire-contract | S1.call | backend/services/wizard/milestones.py + WizardCommitState_v0 (unchanged) | pytest + parity 34/34 unchanged | 1 cell · ms class | Motor | 1 · Deterministic | Owner |
| `akki.backend.wizard_freeze_refuses_when_milestones_not_agreed` | Named surfaces (Wizard) | Wizard freeze endpoint refuses if milestones not agreed → HTTP 422 `{outcome:"refused", reason:"milestones_not_agreed", detail:...}`. FB-4 gate_commit_requires_agreed_milestones. | PROM-S1-refusal-taxonomy-closed | S1.call | backend/routers/wizard_operator.py::freeze | pytest (test_wizard_freeze_refuses_when_milestones_not_agreed) | 1 cell · µs class | FastAPI | 1 · Deterministic | Owner |
| `akki.backend.wizard_freeze_refuses_when_lawful_basis_empty` | Named surfaces (Wizard) | Wizard freeze endpoint refuses via Guard 1 preflight when lawful_basis empty. FB-6 gate_commit_requires_lawful_basis (pre-existing enforcement). | PROM-S1-refusal-taxonomy-closed | S1.call | backend/contracts/wizard_commit_state.py Guard 1 (pre-existing) | pytest (pre-existing wizard tests) | 1 cell · µs class | Pydantic | 1 · Deterministic | Owner |

═══════════════════════════════════════════════════════════════════

*End of v0.7 supplement. 9 rows §T1 + 3 rows §T2 + 4 rows §T3 + 4 rows §T4 + 3 rows §T5 = 23 R4 reflexive rows. Prior source-of-truth files byte-identical. Standing Rule v3 · on-disk canonical.*
