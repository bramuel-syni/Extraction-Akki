# Function & Promise Registry — v0.6 Supplement (Memory Service Stage B · M-G1..M-G9)

**Purpose:** carries the memory-service Stage B (Owner ruling 2026-07-30 cycle 3 option (b) + follow-ups 1a/2a/3a · 2026-07-31) close's own R4 reflexive Registry rows, landed per governance §14 additive-supplement discipline. AC-3 non-negotiable: FPR rows registered in machine YAML BEFORE each function lands. This supplement authoritatively documents the rows; regenerator picks them up on next `tools/registry/regenerate.py` run.

**Source lock:** primary source-of-truth `/app/docs/registry/function_promise_registry_v1.md` byte-identical at SHA `d6ad136f65426c0f86df2227a540aac8142b24dd0cbb015b71ef2991a7a6718a`. Prior supplements byte-identical: v0.1 `2822f99e…` · v0.2 `25c5dd5a…` · v0.3 `8d4cd2ed…` · v0.4 `d1fa1949…` · v0.5 `d2d0c5f4…`.

**Combined source per §14:** `(v0.md + v0.1..v0.5 supplements + v0.6 supplement)` ↔ `registry.yaml` — one set. MRR-G3 round-trip attests transparent extension to N supplements.

**Doctrine reference:** Registry Doctrine v1.0 §3.2 schema · §3.3 R4 · §14 additive-supplement pattern.

**Landed:** 2026-07-31 (atomic Memory Service Stage B commit).

**Governance authority:** Owner ruling 2026-07-30 cycle 3 option (b): "adopt STAGE-A" (frozen contracts + scoped-accessor pattern + working-set eviction constants as [SLOT]s + governed publication ceremony); follow-up decisions 1a (freeze both memory contracts, parity 32→34), 2a (reuse NorthenaLedgerRow_v1 with memory_* data_class values via governed registry version bump — data_class_registry.v3 → v4 additive), 3a (wire /api/memory/* now, engineer-key scoped, server-side scope enforcement).

---

## §S1. Frozen contracts — parity 32 → 34 (two seal events per D4b FREEZE)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.frozen_contract_memory_plane_v0` (Owner (c2) FREEZE) | Named surfaces (memory) | Memory-plane envelope frozen per D4b: (a) crosses environment boundary (integration-key holder ↔ platform); (b) LOW change rate — plane shape is a governed value, not iterated. Snapshot at `tests/invariants/memory_plane_v0.contract_snapshot.json` byte-identical. | PROM-S1-frozen-wire-contract | S1.call | contracts/memory_plane_v0.py + tests/invariants/memory_plane_v0.contract_snapshot.json | pytest (test_frozen_contract_snapshot_parity.py + test_m_g_two_frozen_snapshots_present_and_byte_identical) + `EXPECTED_PARITY = 34` shared counter | 1 cell · µs class | Pydantic v2 | 1 · Deterministic | Owner |
| `akki.memory.frozen_contract_memory_write_back_v0` (Owner (c2) FREEZE) | Named surfaces (memory) | Memory write-back contribution shape frozen per D4b: (a) crosses environment boundary (external contributor ↔ platform); (b) LOW change rate — five-ring shape + class-cap discipline are governed values. Snapshot at `tests/invariants/memory_write_back_v0.contract_snapshot.json` byte-identical. | PROM-S1-frozen-wire-contract | S1.call | contracts/memory_write_back_v0.py + tests/invariants/memory_write_back_v0.contract_snapshot.json | pytest + shared parity counter | 1 cell · µs class | Pydantic v2 · five_rings@v0 shape | 1 · Deterministic | Owner |
| `akki.memory.parity_34_at_stage_b_close` (attest) | Named surfaces (Deviation-audit reflexive) | Two seal events landed atomically; `/api/system/build_info` + `/api/readyz` report `parity_count = 34 = expected_parity`. Every prior V1-G7 assertion bumped 32 → 34 in one atomic parity-restoration commit (invariants + registry). Backend refuses to boot on parity drift (fail-closed). | PROM-S1-frozen-wire-contract | S1.call | services/health/parity_counter.py + tests/invariants/test_frozen_contract_snapshot_parity.py | pytest + integration curl (`/api/readyz` returns 200 with parity 34/34) | 1 cell · µs class | shared parity counter | 1 · Deterministic | Owner |

## §S2. Scoped-accessor pattern — plane isolation by construction (M-G1..M-G4)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.scoped_accessor_binds_plane_at_mint` (M-G1) | Named surfaces (memory) | `ScopedAccessor.__init__` binds `plane_id` + `integration_key` + `tenant_id` + `state` into name-mangled `__slots__`. `for_plane(...)` factory refuses when integration-key mismatch — cross-key access is inexpressible via the accessor seam. | PROM-S1-external-scoped-access | S1.call | services/memory/scoped_accessor.py::for_plane + services/memory/scoped_accessor.py::ScopedAccessor.__init__ | pytest (test_m_g1_scoped_accessor_cannot_read_across_planes_by_direct_call) — break-in raises PlaneScopeViolation | 1 cell · µs class | plane_registry.get_plane | 1 · Deterministic | Owner |
| `akki.memory.scoped_accessor_setattr_immutable_after_init` (M-G2) | Named surfaces (memory · reflexive) | `ScopedAccessor.__setattr__` raises AttributeError post-init. `__slots__` forbids adding new attrs; name-mangled `__plane_id` unreachable from external code; no `plane_id_override` kwarg on any method. Break-in via setattr, dict manipulation, kwarg smuggling all refused. | PROM-S1-external-scoped-access | S1.call | services/memory/scoped_accessor.py::ScopedAccessor | pytest (test_m_g2_scoped_accessor_cannot_bypass_via_kwarg_or_setattr_override) — 4 break-in vectors all refused | 1 cell · µs class | Python __slots__ + name mangling | 1 · Deterministic | Owner |
| `akki.memory.write_back_plane_isolation_by_construction` (M-G3) | Named surfaces (memory) | `write_back.write_contribution` routes through `accessor.insert_contribution`; the accessor cross-checks `contribution_doc.plane_id == accessor.plane_id` and `contribution_doc.tenant_id == accessor.tenant_id`, raising PlaneScopeViolation on mismatch. Foreign-plane forgery via a valid accessor for a different plane refused. | PROM-S1-external-scoped-access | S1.call | services/memory/write_back.py + services/memory/scoped_accessor.py::insert_contribution | pytest (test_m_g3_write_back_write_to_plane_A_isolated_from_plane_B_reads) — forged doc raises PlaneScopeViolation | 1 cell · ms class | scoped_accessor | 1 · Deterministic | Owner |
| `akki.memory.mind_context_never_crosses_keys` (M-G4) | Named surfaces (memory · reflexive) | Working-set partition rows carry `plane_id + tenant_id`; scoped accessor filters both. Direct Mongo query for A's ref filtered by B's plane_id returns empty. Mind context is plane-local by construction. | PROM-S1-external-scoped-access | S1.call | services/memory/scoped_accessor.py::list_working_set + record_working_set_use | pytest (test_m_g4_mind_context_never_crosses_keys) | 1 cell · ms class | Motor client | 1 · Deterministic | Owner |
| `akki.memory.estate_memory_shared_across_keys_via_publication_only` (M-G5 positive control) | Named surfaces (memory) | Registry-visibility (estate-memory) is reachable ONLY through the separate publication ceremony. Post-publication, contribution doc's `rights_class` is widened to `registry_visible` + `intended_scope=registry_publication` + a `registry_ref` is stamped. The write path never sets these values. | PROM-S1-external-scoped-access | S1.call | services/memory/publication.py::attempt_publication | pytest (test_m_g5_estate_memory_shared_across_keys) | 1 cell · ms class | quality gate + rights gate | 1 · Deterministic | Owner |

## §S3. Write-back — five-ring shape + class-cap + rights-at-birth

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.write_back_five_ring_shape_enforced` | Named surfaces (memory) | Every contribution's `five_ring_stamp` must carry all five rings (content · provenance · defensibility · context · re_extraction_handle). Missing rings → `contribution_shape_invalid` governed refusal at the router boundary. | PROM-S1-frozen-wire-contract | S1.call | services/memory/write_back.py::validate_five_ring_shape | pytest — governed refusal envelope | 1 cell · µs class | MemoryWriteBack_v0 | 1 · Deterministic | Owner |
| `akki.memory.write_back_class_cap_never_exceeds_cited_sources` | Named surfaces (Solva) | `class_declared ≤ max(cited_source classes)` per Solva class taxonomy (`fact > utterance > non_factual`). Attempt to publish `class_declared=fact` with `cited_source_classes=[utterance]` refused with `contribution_over_class_cap`. | PROM-S1-frozen-wire-contract | S1.call | services/memory/write_back.py::enforce_class_cap | pytest — 3 cells | 1 cell · µs class | Solva class registry | 1 · Deterministic | Owner |
| `akki.memory.write_back_rights_at_birth_internal_only` | Named surfaces (memory) | Every contribution lands with `rights_class=internal_only` + `intended_scope=mind_context_only` by default. Widening at write-time is refused via `contribution_rights_forbid`. Widening requires the separate publication ceremony. | PROM-S1-frozen-wire-contract | S1.call | services/memory/write_back.py::enforce_rights_at_birth | pytest — 2 cells | 1 cell · µs class | publication ceremony | 1 · Deterministic | Owner |

## §S4. Publication ceremony — governed act, fail-loud on unset [SLOT]

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.publication_quality_threshold_slot_fail_loud` (SR-5) | Named surfaces (memory) | `PUBLICATION_QUALITY_THRESHOLD` is a [SLOT: null] constant. When unset (default posture), the ceremony refuses with `publication_quality_threshold_unset` — fail-loud per SR-5. No silent fallback. No hand-tuned figure passed off as measured. | PROM-S1-frozen-wire-contract | S1.call | services/memory/constants.py + services/memory/publication.py::publication_quality_gate | pytest (test_m_g6_publication_is_separate_governed_act + test_m_g_publication_threshold_unset_by_default) | 1 cell · µs class | AKKI_MEMORY_PUBLICATION_QUALITY_THRESHOLD env var | 1 · Deterministic | Owner |
| `akki.memory.publication_is_separate_governed_act_never_automatic` (M-G6) | Named surfaces (memory) | Publication is never automatic. Attempt → gate → land is a 3-step ledger-recorded ceremony. Refusal at any gate leaves the contribution plane-local (rights_class=internal_only, intended_scope=mind_context_only) — no partial widening. | PROM-S1-frozen-wire-contract | S1.call | services/memory/publication.py::attempt_publication | pytest (test_m_g6) — 3 ledger rows on refused ceremony (attempted + refused; landed absent) | 1 cell · ms class | quality gate + rights gate | 1 · Deterministic | Owner |
| `akki.memory.publication_rights_gate_minimum_cited_sources` | Named surfaces (memory) | Publication gate rejects contributions with `cited_source_count < PUBLICATION_MIN_CITED_SOURCES [SLOT: 1]`. Ensures the class-cap discipline extends through the publication seam. | PROM-S1-frozen-wire-contract | S1.call | services/memory/publication.py::publication_rights_gate | pytest — governed refusal envelope | 1 cell · µs class | services/memory/constants.py | 1 · Deterministic | Owner |

## §S5. Revocation — immediate freeze

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.revocation_freezes_plane_immediately` (M-G7) | Named surfaces (memory) | `revoke_plane` sets `plane.state=revoked` atomically + emits `memory_plane_revoked` ledger row. Any subsequently-minted `ScopedAccessor` sees state=revoked and raises `PlaneRevoked` on read/write. Re-revoke is idempotent (returns `already_revoked=True`). | PROM-S1-frozen-wire-contract | S1.call | services/memory/revocation.py::revoke_plane + services/memory/scoped_accessor.py::_assert_active | pytest (test_m_g7_revocation_freezes_plane_immediately) | 1 cell · ms class | plane_registry.update_plane_state | 1 · Deterministic | Owner |

## §S6. Ledger reconstruction — one append-only ledger per Owner (2a)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.ledger_reuses_northena_ledger_row_v1_with_memory_data_class` (Owner (2a)) | Named surfaces (Northena) | Every memory event rides `NorthenaLedgerRow_v1` (the ONE append-only ledger) with `stamp_audit.data_class ∈ {memory_plane_issued, memory_contribution_landed, memory_contribution_refused, memory_publication_attempted, memory_publication_landed, memory_publication_refused, memory_plane_revoked}`. No second ledger. One trace thread per plane. | PROM-S1-frozen-wire-contract | S1.call | services/memory/ledger.py + services/compliance/deletion_ledger.py::emit_deletion_ledger_row | pytest — every event class produces one row via the shared emitter | 1 cell · ms class | Northena append-only ledger | 1 · Deterministic | Owner |
| `akki.memory.data_class_registry_v3_to_v4_governed_additive` (Owner (2a) 2026-07-31) | Named surfaces (Compliance · reflexive) | Registry bump v3 → v4 lands as a GOVERNED change; v3 file preserved byte-identical; v4 additive with 7 memory_* classes + `authority` block noting Owner ruling + timestamp. Registries are a governed-value class with their own change authority — NEVER an in-place edit. | PROM-S1-frozen-wire-contract | S1.call | services/compliance/data_class_registry.v4.json + services/compliance/deletion_ledger.py::_REGISTRY_PATH | pytest (test_data_class_registry_v3_landed_additive_from_v2 · assertions extended to check v4 additive from v3 + authority block present + 7 memory_* new classes) | 1 cell · µs class | authorized_deletion module | 1 · Deterministic | Owner |
| `akki.memory.plane_state_ledger_reconstructible` (M-G8) | Named surfaces (Deviation-audit reflexive) | Given only the Northena ledger, `ledger_reconstructor.rebuild_state(plane_id)` recomputes: state, revoked_at, issued_at, integration_key, tenant_id, retrieval_scope, contribution counts + IDs, publication counts + published IDs. Attest: delete the plane doc; ledger alone rebuilds the state. | PROM-S1-frozen-wire-contract | S1.call | services/memory/ledger_reconstructor.py | pytest (test_m_g8_plane_state_ledger_reconstructible) — rebuild after registry-doc delete | 1 cell · ms class | Northena ledger `at` sort | 1 · Deterministic | Owner |

## §S7. Router surface — /api/memory/*, engineer-key scoped, server-side enforcement (Owner (3a))

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.router_openapi_registered_eight_endpoints` (Owner (3a)) | Named surfaces (backend) | Eight endpoints registered under `/api/memory/*`: POST /planes; GET /planes; GET /planes/{id}; POST /planes/{id}/contribute; POST /planes/{id}/publish; POST /planes/{id}/revoke; GET /planes/{id}/working_set; GET /planes/{id}/retrieval_scope; GET /planes/{id}/reconstructed_state. Present in OpenAPI at build. | PROM-S1-frozen-wire-contract | S1.call | routers/memory.py + server.py::app.include_router | curl `/api/openapi.json` lists all endpoints | 1 cell · µs class | FastAPI | 1 · Deterministic | Owner |
| `akki.memory.router_scope_enforcement_server_side_per_call` (Owner (3a)) | Named surfaces (memory · reflexive) | Every endpoint (except `/planes` list-issue) enforces plane-scope by (a) fetching plane doc, (b) checking `caller_integration_key == plane.issued_to_integration_key` OR caller has admin/master_admin, (c) refusing with `auth_scope_insufficient` on mismatch. Cross-key HTTP break-in refused with 403. | PROM-S1-external-scoped-access | S1.call | routers/memory.py::_authorize_plane_access | pytest (test_m_g_router_cross_key_plane_read_denied) — 403 auth_scope_insufficient with NO `outcome` key | 1 cell · µs class | services/auth/dependencies | 1 · Deterministic | Owner |
| `akki.memory.refusal_taxonomy_separate_from_auth` (Owner E2) | Named surfaces (memory) | Governed refusals carry `{outcome: "refused", reason, detail}` (8-code closed set: plane_not_found, plane_revoked, plane_scope_invalid, contribution_over_class_cap, contribution_rights_forbid, contribution_shape_invalid, publication_gate_denied, publication_quality_threshold_unset). Auth denials carry `{reason, detail}` with NO `outcome` key. Adding a governed reason requires an Owner ruling. | PROM-S1-refusal-taxonomy-closed | S1.call | services/memory/refusal.py + services/memory/memory_refusal_reasons.v0.json + routers/memory.py | pytest (test_m_g9 × 3 + test_m_g_refusal_reason_set_closed) | 1 cell · µs class | services/auth/auth_refusal.py taxonomy | 1 · Deterministic | Owner |

## §S8. Working-set — usage-proportional, [SLOT]'d constants (Owner (c3))

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.memory.working_set_constants_are_slots` (Owner (c3) SR-5) | Named surfaces (memory · reflexive) | `WORKING_SET_MAX_REFS_PER_PLANE [SLOT: 10_000]` + `WORKING_SET_EVICTION_HALFLIFE_DAYS [SLOT: 30]` + `PUBLICATION_QUALITY_THRESHOLD [SLOT: null]` + `PUBLICATION_MIN_CITED_SOURCES [SLOT: 1]`. Every constant carries a `[SLOT: <default>]` marker in the module docstring. No hand-tuned figure passed as measured. Env-var overridable per SR-5. | PROM-S1-frozen-wire-contract | S1.call | services/memory/constants.py | pytest (test_m_g_constants_all_carry_slot_markers) | 1 cell · µs class | os.environ.get | 1 · Deterministic | Owner |
| `akki.memory.working_set_eviction_is_cache_refresh_not_data_deletion` | Named surfaces (memory · reflexive) | Working-set eviction removes plane-local *references* to artifacts held elsewhere; it does not delete user data or ledger rows. Whitelisted in `test_no_unauthorized_deletion_path.py::_WHITELIST` with rationale preserved inline. Class-distinct from `authorized_deletion` of held data. | PROM-S1-frozen-wire-contract | S1.call | services/memory/working_set.py::evict_by_halflife + tests/invariants/test_no_unauthorized_deletion_path.py | pytest — deletion-path gate green with 2-entry whitelist | 1 cell · ms class | Motor delete_one | 1 · Deterministic | Owner |

## §S9. Standing consequence attest (governance §14 · MRR-E4 β)

This supplement instantiates the pattern ruled in governance §14 applied to Memory Service Stage B: additive supplement beside locked source. v0.md byte-identical at SHA `d6ad136f…` · v0.1..v0.5 supplements byte-identical at prior SHAs. v0.6_supplement is new sibling. MRR-G3's round-trip operates over `(v1.md + v0.1..v0.6 supplements)` ↔ machine form as one set.

═══════════════════════════════════════════════════════════════════

*End of v0.6 supplement. 3 rows §S1 + 5 rows §S2 + 3 rows §S3 + 3 rows §S4 + 1 row §S5 + 3 rows §S6 + 3 rows §S7 + 2 rows §S8 = 23 R4 reflexive rows. Prior source-of-truth files byte-identical. Standing Rule v3 · on-disk canonical.*
