# Function & Promise Registry — v0.9 Supplement (Phase 3 sub-cycle 3)

**Purpose:** carries the Phase 3 sub-cycle 3 (Govern module surfaces) close's R4 reflexive Registry rows, landed per governance §14 additive-supplement discipline. AC-3 non-negotiable: FPR rows registered in machine YAML BEFORE each function lands. This supplement authoritatively documents the 10 rows for the promises implemented in this sub-cycle.

**Source lock:** primary source-of-truth `/app/docs/registry/function_promise_registry_v1.md` byte-identical (SHA unchanged). Prior supplements v0.1..v0.8 byte-identical.

**Landed:** 2026-08-02 (atomic Phase 3 sub-cycle 3 commit).

**Governance authority:** Owner ruling 2026-08-02 (sub-cycle 3 dispatch — Govern module surfaces per Frontend Brief v2 §A2/§A4-2 + Surfaces v2 amendment §A4-2). All rows exclusively consume EXISTING committed endpoints. Zero new frozen contracts; zero new endpoints; parity remains 34/34.

---

## §T1. Govern module surfaces (frontend · read-only over existing endpoints)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.frontend.govern_home_lists_rule_inventory_with_class_chips` | Named surfaces (Govern · frontend) | `/govern` renders a rule inventory table where every row carries a value-class chip (`rails` / `rules` / `engine-settings` / `registries`) AND an enforcement-class chip (`Enforced` / `Attested` / `Monitored`) AND a change-authority label (Compliance countersignature vs Admin sign-off · §A4-2 symmetry). Values render verbatim from `GET /api/compliance/retention_config` and `GET /api/checker/pending`; unset values render as `<MarkedOpenSlot slotName="rule_value_{slug}"/>`. | PROM-S1-external-scoped-access | S1.call | frontend/src/pages/govern/GovernHomePage.jsx | Jest (test_govern_ui_gates.test.js · gate_govern_home_renders_rule_inventory_with_class_chip) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_retention_unset_banner_verbatim` | Named surfaces (Govern · retention) | `/govern/retention` (and `/govern` header) renders the ratified `UNSET_RETENTION_BANNER` string BYTE-IDENTICAL whenever `global_default.retention_window_days === null` OR any held-class posture is `unset`. Never invents copy; never renders a substitute banner. | PROM-S1-refusal-taxonomy-closed | S1.call | frontend/src/pages/govern/GovernRetentionPage.jsx + GovernHomePage.jsx | Jest (test_govern_ui_gates.test.js · gate_retention_unset_banner_verbatim) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_retention_posture_read_only_v2_1_shape` | Named surfaces (Govern · retention) | `/govern/retention` renders the three-held-class table (raw_source · derived · output) with per-class window_days (or MarkedOpenSlot when unset) and posture chip (inheriting/explicit/unset). Consumes `GET /api/compliance/retention_config` verbatim; NO client-side computation over held-class posture. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/govern/GovernRetentionPage.jsx | Jest (test_govern_ui_gates.test.js · gate_retention_read_only_shape) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_change_rule_ceremony_symmetric_direction` | Named surfaces (Govern · change-rule) | `/govern/change-rule` computes direction from numeric deltas: `from>to → loosening (oxblood chip)`; `from<to → tightening (sage chip)`; `==` → neutral (navy). §A4-2 symmetry: loosening requires Compliance countersign; tightening requires Admin countersign. Wrong-capacity attempt at countersign returns HTTP 403 → renders `<AccessControlDeniedPanel>` (navy), NEVER governed refusal. | PROM-S1-refusal-taxonomy-closed | S1.call | frontend/src/pages/govern/GovernChangeRulePage.jsx | Jest (test_govern_ui_gates.test.js · gate_change_rule_ceremony_direction_symmetry) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_change_rule_ceremony_countdown_visible` | Named surfaces (Govern · change-rule) | `/govern/change-rule` renders FB-16 countdown timer (client-side interval; server-side `applies_after_iso` as truth). `Cancel-before-applies` action visible while state ∈ `{proposed, countersigned, waiting}`. Applied rows acquire the ratified `FROZEN_IS_IMMUTABLE` chip. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/govern/GovernChangeRulePage.jsx | Jest (test_govern_ui_gates.test.js · gate_change_rule_countdown_visible) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_refusal_health_coverage_marker_rendered` | Named surfaces (Govern · refusal-health) | `/govern/refusal-health` renders `GET /api/compliance/refusals_coverage` per-family since-dates plus earliest-across-families summary line. Middle-dot separator `·` preserved verbatim per E7. Honest empty-state note when no families surfaced yet — never a "no data" error. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/govern/GovernRefusalHealthPage.jsx | Jest (test_govern_ui_gates.test.js · gate_refusal_coverage_rendered) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_refusal_health_gap_files_via_wizard_door` | Named surfaces (Govern · refusal-health) | Every refusal-family card carries a `File as extraction candidate →` action link routing to `/operator/commission` (the wizard door). FB-10 demand-discovery flywheel: refusal → candidate. Action NEVER absent from a family card. | PROM-S1-frozen-wire-contract | S1.call | frontend/src/pages/govern/GovernRefusalHealthPage.jsx | Jest (test_govern_ui_gates.test.js · gate_refusal_gap_files_via_wizard_door) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_pending_queue_role_aware_filter` | Named surfaces (Govern · pending) | `/govern/pending` calls `GET /api/checker/pending?role={capacity}` where `capacity` is derived from the caller's JWT roles (dpo → compliance; master_admin/admin → admin). §A4-2 seam symmetry: compliance capacity sees admin-initiated tightening; admin capacity sees compliance-initiated loosening. Countersign + Object actions available inline per row. | PROM-S1-refusal-taxonomy-closed | S1.call | frontend/src/pages/govern/GovernPendingPage.jsx | Jest (test_govern_ui_gates.test.js · gate_pending_queue_role_aware) | 1 cell · µs class | React | 1 · Deterministic | Owner |
| `akki.frontend.govern_wrong_capacity_countersign_renders_access_control_denial` | Named surfaces (Govern · pending) | POST `/api/checker/countersign/{id}` from wrong capacity (compliance attempting an admin-required tightening or vice versa) returns HTTP 403 `auth_scope_insufficient` and MUST render via `<AccessControlDeniedPanel>` (navy) NEVER governed refusal (oxblood). Four response classes discipline. | PROM-S1-refusal-taxonomy-closed | S1.call | frontend/src/pages/govern/GovernPendingPage.jsx + GovernChangeRulePage.jsx | Jest (test_govern_ui_gates.test.js · gate_change_rule_ceremony_direction_symmetry) | 1 cell · µs class | React | 1 · Deterministic | Owner |

## §T2. Backend seams (verification-only · no new endpoint)

| function_id | governor | mandate | promise | service_trace | surface | enforcement | cost | dependencies | ladder_rung | owner |
|---|---|---|---|---|---|---|---|---|---|---|
| `akki.backend.govern_surfaces_read_only_no_new_frozen_contract` | Named surfaces (Govern · verification) | The Govern module surfaces this cycle consume EXISTING committed endpoints only: `/api/compliance/retention_config` (GET/POST) · `/api/compliance/refusals_coverage` (GET) · `/api/compliance/refusals` (GET) · `/api/compliance/authorized_deletion` (POST) · `/api/checker/initiate` (POST) · `/api/checker/countersign/{id}` (POST) · `/api/checker/object/{id}` (POST) · `/api/checker/pending` (GET). Zero new endpoints; zero new frozen contracts; parity remains 34/34. | PROM-S1-frozen-wire-contract | S1.call | backend/routers/compliance.py + backend/routers/checker.py (UNCHANGED) | pytest (test_govern_g_g1_to_g_g8.py · g_g1..g_g8) | 1 cell · ms class | FastAPI | 1 · Deterministic | Owner |

═══════════════════════════════════════════════════════════════════

*End of v0.9 supplement. 9 rows §T1 + 1 row §T2 = 10 R4 reflexive rows. Matches Stage A §5 commitment exactly. Prior source-of-truth files byte-identical. Standing Rule v3 · on-disk canonical.*
