# AKKI OS — REMAINING WORK PLAN RWP-1
### 2026-08-01 · The fourth document of the commissioning set
### Travels with: COMMISSIONING PACKAGE CP-1 · LLM CONSUMPTION LEVERS LC v1.0 ·
### CIF STANDING BRIEF v1.0. This plan adds what none of them carries: the CIF
### verification-and-landing track, and the complete path from here to the
### terminal test. It reorders nothing already dispatched (CIF spec §14.3:
### construction order carries no epistemic weight).

Priority statement of record: LLM consumption reduction is the primary
economic objective (CP-1); the terminal test is the singular experiment and
the definition of done (CD-2.1); CIF is in force on adoption (spec §14.1) —
its track below is verification and enforcement-landing, not activation.

---

## TRACKS A–D (as commissioned in CP-1 Part 3 — restated, unchanged)

- **A · Corrections:** record fixes → conformance completions (In progress/
  Ready establish-then-act · non-Canon routes · /api/ask seam) → tier-lock
  bound · stub-figure marking · schedule-assertion copy.
- **B · Consumption levers (priority track):** baseline capture → LC-1 cache
  → LC-2 routing → LC-3 narrowing, each enabled separately, savings
  attributed per lever; A-1 compression at terminal-test approach.
- **C · Evidence machinery:** evidence register + A-2 protocol (must complete
  before the terminal test — the set's one hard ordering constraint) → A-3
  adopted portions → A-4 harness dormant-ready.
- **D · GPU proving:** worker onboarding pack; external stub-parity run when
  the Owner supplies AISCA access.

## TRACK E · CIF VERIFICATION AND ENFORCEMENT LANDING (new in this plan)

Runs parallel to A–D; blocks none of them; establish-then-act throughout.

- **E1 · Build-state verification (first, report-only).** Builder reports
  CIF's mechanics against spec §5–§9 and §12, function by function per the
  Standing Brief §3 table: exists-in-code / partially / absent, with paths.
  The UNVERIFIED column converts to findings. No code lands in E1.
  SUCCESS: every §3 row carries a disk-cited state; contradictions with the
  spec surface as HAZARD-STOPs, not accommodations.
- **E2 · Enforcement landing (sized by E1's findings).** Per spec §12, for
  whatever E1 shows absent: the manifest schema on the five verdict-bearing
  artifact classes with rejection at submission (Brief E5.2); the archive as
  append-only ledger rows + the standing query for unarchived ideas, with
  CIF as entry #1 (E5.3, §14.2); the CR-7 rubric amendment (A5.2); QA
  parameters as queryable/measured fields (§11); the F3 field-binding table
  produced at Stage A for Owner confirmation (E5.1) — unbindable conditions
  fall to rent-date human review per spec, so binding gaps never block.
  SUCCESS: §12's enforcement map holds on disk — an unmanifested verdict
  rejects (gate test); a frame-adoption commit without its M2 sweep record
  fails its cells (gate test); the standing query returns zero
  evaluated-but-unarchived ideas on a clean cycle.
- **E3 · Canon corrections.** Register row + six-engine enumeration amendments
  (Brief C4.1–C4.2), filed as corrections in the honest-record loop.
  SUCCESS: no published document enumerates five engines; the register cites
  the spec and its companion set.
- **E-slots:** O-C1 (QA-e ceiling) and O-C2 (instruction-set confirmation)
  from the Brief — Owner's, non-blocking, convert on confirmation.

## THE PATH FROM CP-1-COMPLETE TO THE TERMINAL TEST

Sequenced by which key unlocks each step; nothing here is backlog — it is the
definition of done unfolding.

1. **OT-1a lands (archive physical facts — Owner/RMS conversation):**
   real archive/CMS reader wiring (V1-I4 as specified in BCR §3.1);
   remaining production packaging tail (PH-R2, destination facts from OT-3).
2. **AISCA access lands (Owner supplies worker-side access):** Track D's
   external stub-parity run executes on AISCA hardware through the seam —
   the pull model proven end-to-end with a worker holding only the
   two-operation credential.
3. **OT-1b lands (grant parameters → topology):** Topology A/B selected;
   HS2 never-rules ratified or struck in the same ruling (closes CC-4);
   if B: transit/purge/rights rulings drafted before dispatch.
4. **OT-2 lands (Hour A + its 300-unit slice, drawn from Hour A,
   uncurated):** BM-V executes in-phase, scored under the A-2 dual-judge
   protocol with kappa on the record (CD-2.1 as amended) — Track C must be
   complete by this point (the one hard ordering constraint).
5. **The terminal test:** one real broadcast hour and one real CMS batch
   flow to qualified units with honest telemetry, zero fixture involvement;
   the V-gate opens through its ceremony; Appendix-A's pending-by-
   construction sentence is removed as the close artifact. The economics
   observability surface (CD-1.3) then holds the first real
   cost-per-qualified-unit decomposition — the number that prices the
   product.

## STANDING RULES CARRIED

Disk is truth; one rulings store; a proposal is not a ruling — provenance
traces on every value; establish-then-act items report before acting;
economizers fail transparent, custody fails closed; no figure quoted
externally without its evidence-register row once Track C lands; nothing in
this plan reorders dispatched work.

## OWNER KEYS, COMPLETE AND IN ONE PLACE

OT-1a archive facts · AISCA worker access · OT-1b grant parameters/topology ·
OT-2 Hour A + slice · OT-3 accounts (LLM, domain+TLS, object store,
data-plane destination) · CP-1 Part 4 slots O-1..O-12 (provisional values
run honestly marked until confirmed) · Brief slots O-C1, O-C2.
Nothing builder-side waits on anything except these.

— END RWP-1 —
